import { StyleSheet } from "react-native";

export default StyleSheet.create({
  enemy: {
    position: "absolute",
    width: 40,
    height: 40,
    backgroundColor: "red",
    borderRadius: 5,
  },
});
